# 公共沙盒页的建立与测试 —— 操作详细指南

> 面向文亚宇宙世界观 Wiki（<https://wenyaverse.fandom.com/zh>）
> 使用 WenyaverseBot + Pywikibot 建立并测试公共沙盒页。

---

## 一、什么是公共沙盒页

沙盒（Sandbox）是供任何人自由试验维基语法、模板、排版的**公共草稿页**，
其内容随时可被覆盖或清空，**不属于正式条目**。惯例上放在项目命名空间下，
本 Wiki 的项目命名空间为「文亚宇宙世界观 Wiki」，别名 `Project:`。

推荐页名（任选其一）：

| 页名 | 说明 |
|------|------|
| `Project:沙盒` | 最常见的中文沙盒页名（推荐） |
| `Project:Sandbox` | 英文习惯页名 |
| `Help:沙盒` | 放在帮助命名空间 |

> 机器人当前权限：`createpage`、`edit`、`move` —— 足以创建和编辑沙盒页。

---

## 二、准备工作（每次运行前确认）

1. **开启 Clash Verge 代理**——系统代理运行在 `127.0.0.1:7897`。
   代理地址已写入 `user-config.py`，脚本会自动读取，但代理软件本身必须开着。
2. **使用虚拟环境的 Python**——即 `.venv/bin/python`，不要用系统 `python3`。
3. **凭据文件就位**——`user-password.py` 存在且有效（含明文密码，已被
   `.gitignore` 忽略，切勿提交或外传）。

先跑一次连接测试确认一切正常：

```bash
.venv/bin/python test_bot_connection.py
```

看到 `🔑 登录状态: 已登录` 即可继续。

---

## 三、建立公共沙盒页

### 方式 A：用脚本自动建立（推荐）

`test_sandbox_edit.py` 支持传入页名。若页面不存在，脚本会自动创建：

```bash
.venv/bin/python test_sandbox_edit.py "Project:沙盒"
```

脚本会：
1. 登录机器人账号；
2. 若 `Project:沙盒` 不存在则新建，写入一段带唯一标记的测试文本；
3. 强制读回页面，校验标记是否存在。

### 方式 B：在浏览器手动建立

1. 打开 <https://wenyaverse.fandom.com/zh/wiki/Project:沙盒>
2. 页面不存在时会提示「创建此页面」，点击进入编辑器；
3. 填入初始内容（示例见下），保存即可。

```wikitext
{{Sandbox}}
<!-- 这是公共沙盒页，欢迎在此自由试验维基语法。此页内容会被定期清理。 -->
```

> 若站点没有 `{{Sandbox}}` 模板，去掉首行即可，不影响使用。

---

## 四、测试沙盒页编辑

建立后，反复运行以下命令即可验证机器人对该页的写入是否正常：

```bash
.venv/bin/python test_sandbox_edit.py "Project:沙盒"
```

**预期输出：**

```text
✅ 已登录：WenyaverseBot
📄 目标沙盒页：Project:沙盒
   页面当前存在
✏️  正在写入测试内容...
🎉 校验成功：写入的标记已在页面中读回，Bot 编辑权限正常！
🔗 查看：https://wenyaverse.fandom.com/zh/wiki/Project:沙盒
```

看到 `🎉 校验成功` 即代表建立与测试均成功。可打开输出的链接在浏览器核对。

---

## 五、常见问题排查

| 现象 | 原因 | 解决 |
|------|------|------|
| 连接卡住 / 超时 | Clash Verge 未开启或端口不对 | 确认代理运行在 `127.0.0.1:7897` |
| `credentials could not be authenticated` | BotPasswords 名字/密码不匹配 | 到 `Special:BotPasswords` 核对并更新 `user-password.py` |
| `No module named 'pywikibot'` | 用了系统 python | 改用 `.venv/bin/python` |
| 提示无权限编辑 | 页面被保护 / 账号权限不足 | 换用未保护的沙盒页，或向社区申请权限 |
| 编辑不在「最近更改」折叠显示 | 账号无 `bot` 用户组标识 | 不影响功能；如需折叠，向管理员申请 Bot Flag |

---

## 六、清理沙盒（可选）

沙盒内容按惯例可随时覆盖或清空。手动清空：在浏览器编辑该页，删除内容后保存。
也可另写脚本将其重置为初始模板文本。
