# ============================================
# macOS 系统文件
# ============================================
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
Icon?

# ============================================
# 编辑器 / IDE
# ============================================
.vscode/
.idea/
*.swp
*.swo
*~

# ============================================
# 日志与临时文件
# ============================================
*.log
*.tmp
*.bak

# ============================================
# AI 工具临时产物 (Gemini / Claude / GPT Code 等)
# ============================================
gemini-code-*.html
claude-code-*.html
chatgpt-code-*.html
*.tmp.html

# ============================================
# 依赖目录（如后续引入前端工具链）
# ============================================
node_modules/
dist/
.cache/

# ============================================
# Pywikibot 敏感凭据（切勿提交）
# ============================================
user-password.py

# Pywikibot 运行时生成文件
throttle.ctrl
apicache*/
*.lwp

# ============================================
# Python
# ============================================
__pycache__/
*.py[cod]
.venv/
venv/
# .venv 在部署机上是指向另一目录的符号链接，'.venv/' 只匹配目录、匹配不到链接，
# 补一条无斜杠规则，否则 git status 会把整个虚拟环境当新文件列出来。
.venv
# 手工备份（offline_alert.py.bak-0804-1837 之类）。上面的 '*.bak' 匹配不到 .bak-日期 后缀。
*.bak-*

# ============================================
# 会话导出（Claude /export 生成，非项目内容）
# ============================================
*-bash-input.txt

# ============================================
# 压缩包（skill 等已解压入库，压缩包本身不入库）
# ============================================
*.zip

# ============================================
# 环境变量 / 密钥（含 API key，切勿提交）
# ============================================
.env
*/.env

# bilibili_notify 推送位置状态（运行时生成）
bili_state.json
