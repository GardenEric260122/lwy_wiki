# 文亚宇宙世界观 Wiki —— 内容与样式分析报告

> 抓取时间基准：MediaWiki 1.43.9 | 抓取工具：`fetch_wiki_content.py`
> 本地快照目录：`wiki_dump/`

---

## 一、抓取概况

| 类型 | 数量 | 保存位置 |
|------|------|----------|
| 条目正文 | 60 篇 | `wiki_dump/articles/` |
| 模板 | 91 个 | `wiki_dump/templates/` |
| MediaWiki 界面页 | 36 页 | `wiki_dump/mediawiki/` |
| 分类 | 16 个 | `wiki_dump/categories.txt` |
| 站点配置 | 1 份 | `wiki_dump/siteinfo.json` |

- 全站条目总量约 **26 万字节**，平均每篇约 4.3 KB，最大条目「孙笑川教授」约 16 KB。

---

## 二、样式（CSS）分析 —— 与项目 `wiki2-v2.0.0.css` 的关系

**核心结论：线上 `MediaWiki:Common.css` 与项目中的 `李文亚Wiki/wiki2-v2.0.0.css` 内容完全一致。**

- 逐行 `diff` 仅有一处差异：线上版本文件末尾**缺少一个换行符**，CSS 规则本身 100% 相同。
- 说明线上部署的桌面版样式确实就是本项目的 **v2.0.0**，版本同步无偏差。

> 维护建议：今后修改样式时，只需在项目里更新 `wiki2-vX.Y.Z.css`，
> 再将内容整体粘贴/推送到 `MediaWiki:Common.css` 即可保持一致。
> （可进一步写一个同步脚本，用 Pywikibot 直接把本地 CSS 推送到 Common.css。）

---

## 三、内容编写风格分析

### 3.1 条目结构高度模式化

抓取的 60 篇条目呈现清晰、统一的「人物百科」范式：

- **信息框**：30 篇条目（占人物类条目绝大多数）使用统一的 `{{Character}}` 信息框。
- **开篇**：信息框后紧跟一句**加粗全称 + 外文名 + 生卒/身份**的定义句，仿维基百科体例。
  例：`'''艾路迪克教授'''（Professor Aludick，1973年－），英国物理学者……`

### 3.2 章节惯例（按出现频次）

| 章节标题 | 出现次数 | 用途 |
|----------|----------|------|
| `== 相关条目 ==` | 30 | 末尾的内部链接列表（几乎每篇必有）|
| `== 评价 ==` | 26 | 支持者 / 批评者双方观点 |
| `== 生平 ==` | 21 | 人物传记主体 |
| `== 外部链接 ==` | 7 | 站外引用 |
| `== 历史 == / == 参见 ==` | 6 | 机构类条目常用 |
| `== 主要成果 == / == 研究方向 ==` | 4-5 | 学术人物专用 |

> 典型人物条目骨架：**信息框 → 定义句 → 生平 → 研究成果 → 争议 → 支持/评价 → 相关条目**。

### 3.3 常用模板（按调用频次）

| 模板 | 调用次数 | 说明 |
|------|----------|------|
| `{{Character}}` | 30 | 人物信息框（Portable Infobox 格式）|
| `{{formatnum}}` | 4 | 数字格式化 |
| `{{SITENAME}}` / `NUMBEROF*` | 多处 | 魔术字，多见于主页/统计 |
| `{{Event}}` | 2 | 事件类信息框 |
| `{{University}}` / `{{Region}}` / `{{Country}}` | 各 1 | 机构/地理信息框（尚不成熟）|

### 3.4 写作语气

- 采用**百科式中立叙述**框架（「据称」「支持者认为」「批评者则认为」），
  但题材本身是围绕「李文亚」的**戏仿/亚文化叙事**，人物与事件为虚构或夸张演绎。
- 大量内部链接 `[[...]]` 构成条目间的引用网络，世界观自洽性强。

---

## 四、核心模板详解：`{{Character}}`

采用 Fandom 的 **Portable Infobox**（`<infobox>` XML 语法），分三段：

1. **标题+图片**：`title`（默认取页名）、`image` + `caption`
2. **基本信息组**：外文名、出生日期、出生地、职业/身份
3. **任职与研究组**：所属机构、主要职务、同僚、研究领域、主要争议对象

> 该信息框的视觉呈现（渐变标题、卡片阴影）正由 `wiki2-v2.0.0.css` 中的
> **Portable Infobox 重构**样式模块负责——内容模板与样式表在此形成对应关系。

---

## 五、站点配置要点（`siteinfo.json`）

- **平台**：MediaWiki 1.43.9（Fandom 托管）
- **项目命名空间**：`文亚宇宙世界观 Wiki`（别名 `Project:`）
- 启用了 Fandom 全套社交扩展：留言墙(1200)、博客(500)、论坛(110)、地图(2900)、GeoJson(420)、Module/Lua(828) 等。

---

## 六、可延伸的自动化方向

基于本次抓取能力，后续可考虑：

1. **CSS 同步脚本** —— 一键把 `wiki2-vX.Y.Z.css` 推送到 `MediaWiki:Common.css`。
2. **信息框补全检查** —— 扫描使用 `{{Character}}` 的条目，报告缺失字段。
3. **孤立/断链检测** —— 分析 `[[...]]` 链接网络，找出未创建的红链条目。
4. **章节规范巡检** —— 对照标准骨架，提示缺少「相关条目」「评价」等惯例章节的条目。
